---
name: paper-overview-figures
description: >-
  基于项目代码和本地参考论文绘制、重绘或优化科研论文 overview、方法与系统架构图。
  适用于 ACL/EMNLP 风格、LLM Harness、训练流程、低文字密度、轻漫画与手绘模块、
  核心公式保留，以及中英双版矢量 PDF/SVG 交付；不用于普通业务流程图或实验结果统计图。
---

# 科研论文 Overview 绘图

## 目标与优先级

先理解真实实现，再将方法组织成适合论文的视觉叙事，而不是把代码调用链翻译成方框和箭头。

遵循：**科学准确性 > 关键机制完整性 > 可读性 > 视觉简洁 > 装饰性。**

默认风格为：参考项目中的 ACL/EMNLP 论文插图，少文字、强层次、模块采用简洁漫画式示意，加入克制的手绘感，并在重点模块保留必要计算与公式。这是用户的设计偏好，不是会议官方格式规范。

## 输入与默认值

- `project_root`：使用用户指定的项目路径；未指定时使用当前项目根目录，不写死历史项目路径。
- `references_dir`：默认 `<project_root>/papers`。
- `output_dir`：默认 `<project_root>/figure`，不存在则创建；保留已有文件。
- `focus`：优先梳理 LLM Harness 与训练流程；项目不存在这些模块时，改为实际核心方法，不强行套用。
- `languages`：默认分别生成英文与中文版本，不在同一张图中堆叠双语标签。
- `target_size`：优先读取当前论文模板或用户要求；未提供时选择合理尺寸并记录假设。

尊重用户明确覆盖的设置。读取已有图、源文件和反馈，延续已认可的设计。路径不可访问、参考论文缺失或实现不完整时，明确记录限制，不声称已阅读；继续完成有依据的部分，不伪造项目专属结构。

## 1. 阅读实现，建立证据

阅读 README、入口脚本、核心模块、配置和相关训练代码；按需要检查少量测试、日志或样例。只做理解与制图所需的检查，不启动完整训练、付费 API 调用或大规模数据处理，不擅自修改业务代码。

对 LLM Harness，核实实际存在的输入与状态、上下文构造、模型调用、动作解析、工具或环境执行、反馈、校验与终止条件。先确认它是交互运行框架、评测框架还是其他类型，不凭名称推断功能。

对训练流程，核实数据或轨迹来源、预处理与筛选、监督或奖励信号、训练目标、参数更新，以及训练后模型如何参与推理。区分训练、推理与评测，明确可训练和冻结组件；只在代码有依据时绘制闭环或梯度路径。

为关键模块、连接和公式建立简短证据表，记录 `文件路径:函数/类/配置项`，必要时附行号。区分“代码确认”“仅文档描述”“推断/待确认”；代码与文字冲突时注明差异。未经确认的机制不作为既成事实进入最终图。不得自行补入 PPO、GRPO、奖励模型、自反思或其他常见但未实现的步骤。

## 2. 观察参考论文的真实图像

优先查看 `references_dir` 中相关论文的 overview 或方法图，通常选取 2–4 个有代表性的参考。必须渲染或截图查看对应 PDF 页面，不能只依赖提取的正文判断视觉风格。

记录文件名、页码或图号，以及准备借鉴的特征：布局、模块隐喻、文字密度、配色、线条、公式位置与视觉重点。借鉴设计语言，不复制整幅插图，也不把参考论文的方法当作本项目实现。

缺少参考时使用本 skill 的默认风格，并明确未完成论文风格对照，不虚构参考来源。

## 3. 确定主线与不可丢失的信息

先写一句话方法概述，确定最重要的 1–3 个、有证据支持的贡献点，并形成“关键细节保留清单”。清单覆盖理解方法所必需的输入输出、核心模块、重要中间表示、训练信号、关键计算及它们的实际关系。

按三层组织信息：全局层说明整体方法；机制层展开最重要的 1–2 个模块；细节层仅保留有解释价值的公式、符号或微型示例。可用并列子图、局部放大或共享对象组织 Harness 与训练，但必须反映真实关系。

删除冗长说明、重复标签、文件名和非关键工程细节，不删除让方法区别于通用流水线的机制。正文级解释移到图注或设计记录。所有布局和模块数量均以真实方法为准，不为套模板而增删阶段。

## 4. 绘制简洁、自然的科学插图

采用“整体严谨、模块内轻漫画化”的表达。每个模块优先用与功能对应的小场景或简洁矢量图标，例如文档、表格、工具、状态卡片、轨迹片段或参数更新示意，而不是只放一个文字方框。

用空间分区、对象交互和少量必要连接表达关系，避免密集的等大小矩形、菱形判断节点、层层嵌套和满屏箭头。保留解释数据流、控制流或参数更新所必需的连接；不同含义采用一致的线型并在必要时给出小图例，不能只靠颜色区分。

突出核心贡献模块：给予更大的空间、更清晰的边界或一种强调色，弱化常规模块。默认使用白色或浅色背景、少量低饱和配色、深色文字和充足留白。

将轻微不规则轮廓、柔和曲线和手绘标记限制在插画与装饰中。文字、公式、箭头方向和整体对齐保持清楚稳定。不要使用随意表情符号、夸张卡通人物或装饰替代科学含义。

标签尽量简短，英文通常为 2–5 个词，中文按相同含义精简。按最终排版尺寸检查字号，默认以约 8–10 pt 为设计起点，而非会议硬性规则；宁可调整布局，也不要不断缩小文字。

## 5. 在重点模块保留计算与公式

仅加入解释核心机制所必需且有证据支持的计算，例如实际使用的目标函数、评分、选择、聚合或更新关系。优先在相关模块旁放置 1 个紧凑公式，必要时增加，不给每个模块机械地配公式。

核对符号、求和范围、归一化、优化方向和参数含义与实现一致。简化公式时注明它是等价形式还是概念示意，不把示意写成精确实现。缺少证据时用准确的操作描述，不编造公式。

保持全文与图内符号统一，为理解所必需的变量提供短标签。Harness 执行反馈、训练信号和参数更新不得混淆，更不能暗示对不可微环境进行了反向传播。

## 6. 同源生成中英版本与可编辑文件

两种语言共享布局、模块编号、配色、图标、箭头和数学符号，文字来自独立的中英术语表。翻译保持语义与术语一致，允许因文字长度进行小范围排版调整，但不改变结构或信息量。

选择环境中可用的矢量工具链，优先保留可编辑 SVG，并导出矢量 PDF；可使用 SVG/Inkscape 或能可靠产生等价结果的程序化工具。不要将整张位图放入 PDF 后称为矢量图。

将图形、文字和连接分组，给重点模块保留清晰的对象标识。中文字体须覆盖全部字符；检查 PDF 字体嵌入或字形轮廓，避免乱码，同时保留文字可编辑的源文件。不附带字体文件。

保留统一布局配置、双语标签和可复现生成脚本或明确的导出步骤。记录工具与依赖；使用手绘随机扰动时固定种子。已有人工编辑的 SVG 不得被脚本无提示覆盖。

## 7. 渲染检查与非退化迭代

完成后实际渲染两版 PDF，在最终论文显示尺寸及放大视图下检查，不以“文件已生成”替代验收。

至少完成三类检查：科学内容与证据一致；视觉上清晰、简洁且有重点；中英版本、字体与导出结果正确。检查遮挡、裁切、线条穿字、公式可读性、留白、颜色对比，以及灰度情况下的可辨识性。

每轮优化都对照“关键细节保留清单”，并保留可回退版本：

- “太像流程图”：改变构图和模块表达，减少无意义边框，不删除必要逻辑关系。
- “文字太多”：改用短标签、符号、微型示例或图注，不抹去核心机制。
- “关键细节丢失”：在重点模块恢复计算、公式或中间表示，不把整张图重新塞满说明。
- “更自然、增加手绘感”：微调插画轮廓与装饰，不牺牲公式、文字和连接的准确性。

同步修订两种语言。保留已通过检查的结构，避免因一次局部修改重做整个布局。不能进行视觉检查时明确标注未验证，不声称已经完成出版级验收。

## 8. 交付

默认在 `output_dir` 下提供：

- `overview_en.pdf`、`overview_zh.pdf`：正式矢量输出。
- `overview_en.svg`、`overview_zh.svg`：可编辑源图。
- `overview_en.png`、`overview_zh.png`：预览，不替代矢量交付。
- `src/`：生成脚本、共享布局与双语标签；手工工作流则提供源图和导出步骤。
- `README.md`：方法概述、代码证据、论文风格参考、关键细节保留清单、中英图注、复现方法、验收结果及已知限制。

只在授权的输出范围内创建或修改文件，不删除用户已有内容，不上传私有代码或论文到外部服务。

最终回复列出实际生成文件的路径，简要说明重点模块、保留的核心计算、验证情况及未解决的问题。若用户要求实际制图，不要只交付规划、提示词或一段无法复现的设计描述。
